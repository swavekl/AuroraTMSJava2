import { DiscountedPriceCalculator } from './discounted-price-calculator';

describe('DiscountedPriceCalculator', () => {
  it('should create an instance', () => {
    // expect(new DiscountedPriceCalculator()).toBeTruthy();
  });
});
